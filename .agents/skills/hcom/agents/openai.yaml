interface:
  display_name: "Hcom"
  short_description: "Repo-specific patterns and workflows for hcom"
  default_prompt: "Use the hcom repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true